# 复制为 .env 后修改
# cp .env.example .env

SECRET_KEY=change-me-to-a-long-random-string
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me

HOST=0.0.0.0
PORT=8000

# 本地开发默认使用 minimal-backend/ 目录
# DATA_DIR=./data

# 生产同域托管前端时启用（见 scripts/run-prod.py）
# STATIC_DIR=../minimal-frontend/dist
# SERVE_STATIC=true

# CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080
